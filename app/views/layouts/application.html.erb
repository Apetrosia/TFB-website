<!DOCTYPE html>
<html class="h-full">
  <head>
    <title>TheFirstBogatyr</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="flex h-full flex-col min-h-screen w-screen">
    <main class="container mx-auto flex flex-col ">
      <%= yield %>
    </main>
  </body>
</html>
